import styled from "styled-components";

export default styled.button`
  background-color: var(--white);
  border: none;
  position: absolute;
  top: 0.8rem;
  right: 1.2rem;

  & svg {
    height: 25px;
    width: 25px;
  }
`;
