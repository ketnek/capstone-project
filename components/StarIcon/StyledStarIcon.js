import styled from "styled-components";

export default styled.button`
  background-color: var(--white);
  border: none;
  position: absolute;
  top: 0.2rem;
  right: 0.1rem;

  & svg {
    height: 1.2rem;
    width: 1.2rem;
  }
`;
